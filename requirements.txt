# ReBrewie Control Pi — requirements.txt
# Updated for Python 3.13 compatibility (Raspberry Pi 4B / armv6l / armv7l / aarch64)
#
# Root cause of original failure:
#   pydantic==2.7.4 → pydantic-core==2.18.4 → PyO3 v0.21.2 (max Python 3.12)
#   Python 3.13 on the Pi has no pre-built ARM wheel; source compile fails.
#
# Fix applied:
#   • pydantic upgraded to 2.10.6  → pydantic-core 2.27.2 → PyO3 0.23+ (Python 3.13 ✅)
#   • pydantic-settings bumped to match
#   • uvicorn[standard] → uvicorn  (drops uvloop/httptools compile; websockets
#     and .env support come from the packages already listed below)
#   • httpx, jinja2, websockets bumped to current stable — all pure-Python or
#     have armv6l wheels on piwheels for cp313

fastapi==0.115.6
uvicorn==0.32.1
pydantic==2.10.6
pydantic-settings==2.7.0
jinja2==3.1.5
python-multipart==0.0.20
pyserial==3.5
httpx==0.28.1
websockets==14.1
blinkpy==0.25.6
